# .ai

This folder owns per-tool AI assistant configuration for tools other than Claude Code (Cursor, Windsurf, Codex, Gemini CLI). Status: Planned.

The contract every tool follows is [AGENTS.md](../AGENTS.md) at the repo root. Files here only adapt that contract to a specific tool's config format; they never restate or extend it.
