# Scripts

This folder owns repo maintenance and one-off operational scripts. Status: Planned.

Anything that graduates from one-off to routine belongs in `tools/` or a workspace package script instead.
