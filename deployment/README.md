# Deployment

This folder owns deployment and release artifacts: environment definitions, rollout configuration, and deploy checklists as code. Status: Planned.

The deployment SOP lives in [docs/05-operations/deployment.md](../docs/05-operations/deployment.md). Per-app Vercel configuration stays inside each app.
