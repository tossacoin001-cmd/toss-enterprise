# toss-visibility (MCP server)

Status: Proposed, not built. Exposes visibility-check functions (from the shared checks package, see the agent specs) as MCP tools so audits can run from any AI client.

Policy and registry: [docs/03-ai/mcp-servers.md](../../docs/03-ai/mcp-servers.md). Build order: spec in that doc first, read-only by default, ADR for anything beyond read-only.
