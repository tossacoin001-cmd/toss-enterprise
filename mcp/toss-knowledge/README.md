# toss-knowledge (MCP server)

Status: Proposed, not built. Read-only server exposing `docs/` search to any AI tool, so assistants outside this repo can query the Knowledge Brain.

Policy and registry: [docs/03-ai/mcp-servers.md](../../docs/03-ai/mcp-servers.md). Build order: spec in that doc first, read-only by default, ADR for anything beyond read-only.
