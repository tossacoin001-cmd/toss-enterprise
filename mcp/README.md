# MCP

This folder owns MCP (Model Context Protocol) server implementations and their configuration. Status: Planned.

MCP policy and usage rules live in [docs/03-ai/](../docs/03-ai/). A server that becomes a shared dependency graduates to a workspace package.
