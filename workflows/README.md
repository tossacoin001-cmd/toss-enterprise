# Workflows

This folder owns business automation workflow definitions and exports (n8n and similar). Status: Planned.

It supersedes the previously planned `automation/` folder (ADR-0008). CI/CD workflows stay in `.github/workflows/`; agent behavior stays in `agents/`.
