# Configs

This folder owns shared configuration that is not a workspace package: service config templates, environment variable templates, and tool presets. Status: Planned.

Publishable config packages (tsconfig, eslint presets) stay planned in `packages/config`. Real secrets never live anywhere in this repo; see [docs/02-engineering/environments-and-secrets.md](../docs/02-engineering/environments-and-secrets.md).
