# Tools

This folder owns internal command-line tools and developer utilities. Status: Planned.

A tool is something you run; a package is something you import. Shared importable code belongs in `packages/` per [monorepo.md](../docs/01-architecture/monorepo.md).
